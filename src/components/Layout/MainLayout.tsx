export const MainLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div>
      MainLayout - Main HARDKODED
      {children}
    </div>
  );
};

MainLayout.displayName = 'MainLayout';
